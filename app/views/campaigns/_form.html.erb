<%= form_for(@campaign) do |f| %>
  <% if @campaign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campaign.errors.count, "error") %> prohibited this campaign from being saved:</h2>

      <ul>
      <% @campaign.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :sync_with_salesforce %><br>
    <%= f.check_box :sync_with_salesforce, :disabled => !@campaign.client.is_connected_to_salesforce %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
